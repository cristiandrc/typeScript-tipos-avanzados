//arrays fuertemente tipados definimos que tipo de dato tiene un array y en que posicion
const user: [string, number, boolean] = ["Irving", 20, true]

const [username,age] = user
console.log(username)
console.log(age)
